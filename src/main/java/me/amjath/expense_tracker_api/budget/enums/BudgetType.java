package me.amjath.expense_tracker_api.budget.enums;

public enum BudgetType {
    /** A single monthly budget covering all expenses. */
    GLOBAL,
    /** A budget scoped to a specific expense category. */
    CATEGORY
}
